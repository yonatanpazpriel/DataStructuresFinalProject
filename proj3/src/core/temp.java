/*
int currQuad = -1;
        HashMap<Integer, Integer> quadToAmountFilled = new HashMap<>();
        for (int i = 0; i < WIDTH; i++) {
            for (int j = 0; j < HEIGHT; j++) {
                boolean marked = world[j][i] != 0;
                if (i < WIDTH / 2 && j < HEIGHT / 2 && marked) {
                    topLeftQuad++;
                } else if (i > WIDTH / 2 && j < HEIGHT / 2 && marked) {
                    topRightQuad++;
                } else if (i < WIDTH / 2 && j > HEIGHT / 2 && marked) {
                    bottomLeftQuad++;
                } else if (i > WIDTH / 2 && j > HEIGHT / 2 && marked) {
                    bottomRightQuad++;
                }
            }
        }
        quadToAmountFilled.put()
 */